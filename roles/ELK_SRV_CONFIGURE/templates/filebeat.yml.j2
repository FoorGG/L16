# Filebeat configuration generated by Ansible

filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/*.log

output.logstash:
  hosts: {{ beats_logstash_hosts }}
  ssl.certificate_authorities: ["{{ beats.certs_path }}/{{ beats.ca_cert }}"]
  ssl.enabled: true
