# Filebeat configuration generated by Ansible

filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/*.log

output.logstash:
  hosts: {{ beats_logstash_hosts }}
  ssl.certificate_authorities: ["{{ beats_certs_path }}/{{ beats_http_ca_crt }}"]
  ssl.enabled: true
