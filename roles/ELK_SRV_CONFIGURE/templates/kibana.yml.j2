# Kibana configuration generated by Ansible

server.port: 5601
server.host: "0.0.0.0"

elasticsearch.hosts: {{ kibana_elasticsearch_hosts }}
elasticsearch.ssl.certificateAuthorities: ["{{ kibana_certs_path }}/{{ kibana_http_ca_crt }}"]
elasticsearch.username: "{{ kibana_user }}"
elasticsearch.password: "{{ kibana_password }}"
