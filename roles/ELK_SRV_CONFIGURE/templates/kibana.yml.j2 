# Kibana configuration generated by Ansible

server.port: 5601
server.host: "0.0.0.0"

elasticsearch.hosts: {{ kibana.elasticsearch_hosts }}
elasticsearch.ssl.certificateAuthorities: ["{{ kibana.certs_path }}/{{ kibana.ca_cert }}"]
elasticsearch.username: "{{ kibana.user }}"
elasticsearch.password: "{{ kibana.password }}"
